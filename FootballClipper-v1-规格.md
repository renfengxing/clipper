# FootballClipper v1 规格文档

> 给青少年足球家长用的本地视频标记+剪辑工具。
> 第一期：纯工具，不做云、不做社区。

---

## 一、项目定位

**用户**：拍摄孩子足球比赛的家长（兼业余教练观察者）

**核心痛点**：原视频很长（15-30分钟），需要按时间段截取出几十个有意义的片段，每个片段配一个分析性标题（"宽宽过人没体力，康康拿球打门歪了"这种），用于复盘、给教练、归档。

**目标**：把"标记 → 命名 → 剪辑"这个循环做到极致顺手。一场比赛标30个片段，10分钟内搞定。

**明确不做**：云存储、社交分享、自动事件检测、球员管理、统计图表、转场特效、多视频合并、字幕画板。

---

## 二、技术栈

| 层 | 选型 |
|---|---|
| 桌面壳 | Electron 30+ |
| UI 框架 | React 18 + TypeScript |
| 样式 | Tailwind CSS |
| 状态管理 | Zustand |
| 视频播放 | HTML5 `<video>` |
| 剪辑引擎 | ffmpeg 二进制（sidecar 打包） |
| 语音识别 | Web Speech API（Chromium 内置，中文） |
| AI 文案 | Anthropic Claude API |
| 项目存储 | JSON 文件（与源视频同目录） |
| 打包 | electron-builder → `.dmg` |
| 脚手架 | electron-vite |

**目标平台**：macOS 优先；代码保持跨平台 friendly，以后能直接打 Windows 版。

---

## 三、功能清单（v1 必做 13 项）

1. 打开本地 mp4/mov 视频（拖入或菜单）
2. 视频播放器：播放/暂停/快进/慢放/单帧步进
3. 时间线（timeline）：拖动游标、缩放、显示已标记片段色条
4. 标入点 / 标出点（快捷键 + UI 按钮）
5. 片段列表：标题、起止时间、时长
6. 标题编辑（列表内直接改）
7. 片段重排序、删除
8. 语音输入标题（按住 `M` 说话）
9. AI 整理文案（Claude API 润色为标准命名格式）
10. 项目自动保存（防抖写 JSON）
11. 一键批量导出（ffmpeg `-c copy` 秒级精度）
12. 命名冲突自动加 `_02 _03`
13. 设置页：API Key、标题模板

---

## 四、UI 布局

```
┌──────────────────────────────────────────────────────────────────┐
│ [打开] [保存] [设置]                          项目: DJI_xxx.mp4   │
├──────────────────────────────────┬───────────────────────────────┤
│                                  │ 片段列表 (5)        [📦 导出]  │
│                                  │ ───────────────────────────── │
│         视频播放器                │ ▶ 防守没卡住人                │
│         (HTML5 <video>)          │   04:12 - 04:22   (10s)  ✏️ 🗑│
│                                  │ ─────────                     │
│                                  │   过3人鞋掉了                 │
│                                  │   07:31 - 07:39    (8s)  ✏️ 🗑│
│                                  │ ─────────                     │
│                                  │   ...                        │
├──────────────────────────────────┤                              │
│  ◀◀  ▶  ▶▶   [I入] [O出] [M语音] │                              │
│  时间线 ━━[■■]━━━[■■]━━[■]━━━━  │                              │
│  00:00 ────────────────── 25:43  │                              │
├──────────────────────────────────┴───────────────────────────────┤
│  状态: 已保存 14:32:05          ffmpeg 路径: ./bin/ffmpeg ✓      │
└──────────────────────────────────────────────────────────────────┘
```

**布局规则**

- 左右分栏，左侧视频区占约 65%，右侧片段列表约 35%
- 视频区下方是时间线 + 控制条
- 右上角是导出按钮（不再放页面底部）
- 底部状态栏显示自动保存时间和 ffmpeg 健康检查

---

## 五、快捷键

| 键 | 动作 |
|---|---|
| `Space` | 全局：播放/暂停；选中片段时：预览该片段（入点→出点→暂停） |
| `J` / `K` / `L` | 倒放 / 暂停 / 快进（连按加速 1x→2x→4x→8x） |
| `←` `→` | 单帧步进（约 0.033s） |
| `Shift+←` `Shift+→` | 跳 5 秒 |
| `I` | 标入点（在当前播放位置） |
| `O` | 标出点（在当前播放位置） |
| `Enter` | 保存当前片段并弹出标题输入框 |
| `M`（按住） | 语音录入标题（松开停止录音 → Whisper/Web Speech 转文字 → Claude 整理） |
| `Cmd+E` | 导出全部片段 |
| `Delete` | 删除选中片段 |
| `Esc` | 取消当前 I/O 标记 或 取消选中片段 |

---

## 六、核心用户流程

### 6.1 标记一个新片段

```
1. 视频播放到 04:11
2. 按 I               → 时间线出现绿色 ▼，浮窗显示「入点：04:11.84」
3. 继续播到 04:22
4. 按 O               → 时间线出现红色 ▼，I/O 之间黄色高亮
                       浮窗显示「准备保存：04:11.84 → 04:22.12（10.28s）」
5. 按 Enter           → 屏幕中央弹出标题输入框
6. 三种填法任选：
   A. 直接打字「防守没卡住人」+ Enter
   B. 按住 M 说话「防守没卡住人没顶到人」→ 松开
      → Web Speech 转文字
      → Claude 整理为「防守没卡住人」
      → 你确认 + Enter
   C. 直接 Enter 跳过命名 → 列表里显示「未命名片段」
7. 片段加入右侧列表，时间线黄条变蓝条
```

### 6.2 调整已保存的片段

**点击列表里的片段**：
- 播放头跳到该片段入点
- 视频自动暂停在入点的第一帧（不自动播放）
- 时间线滚动定位、色条高亮为激活色（其他变灰）
- I/O 标记重新显示在时间线上，可立刻拖拽

**拖拽 I/O 标记**：
- 拖动时视频画面实时 scrub
- 显示气泡 `04:12.30`（实时更新）
- 入点不能拖到出点之后（自动阻挡）
- 松手即保存

**拖拽热区**：
- 时间线上 `▼` 标记本体
- 色条左端（改入点）/ 右端（改出点）
- 色条中间整体（平移整段，时长不变）

**精细调整**：单击端点选中后，`←` `→` 单帧微调

**预览片段**：选中后按 `Space`，从入点播到出点自动暂停

**修改标题**：点列表里的标题直接编辑，失焦或 Enter 自动保存

**删除**：选中后 `Delete` 或点 🗑

### 6.3 批量导出

点右上 `📦 导出` 按钮 → 弹窗：

```
┌─────────────────────────────────────┐
│  导出 5 个片段                       │
│  ──────────────────────────────     │
│  输出目录：                          │
│    ◉ 与源视频同目录                  │
│    ○ 自定义...                      │
│                                     │
│  命名冲突：自动加 _02 _03           │
│                                     │
│  ☑ 跳过已存在的同名文件             │
│                                     │
│  [取消]              [开始导出]      │
└─────────────────────────────────────┘
```

点开始 → ffmpeg 后台跑 → 进度条显示「3/5 防守没卡住人.mp4」→ 完成提示。

**ffmpeg 调用**：
```
ffmpeg -ss <in> -i <source> -t <duration> -c copy <output> -y
```
（秒级精度，不重编码，速度极快）

---

## 七、数据结构

### 7.1 项目文件 `<视频名>.kkfb.json`

与源视频同目录，便于一起移动备份。

```json
{
  "version": "1.0",
  "source_video": "DJI_20260531162117_0023_D.MP4",
  "video_duration": 1234.5,
  "created_at": "2026-06-01T12:00:00Z",
  "updated_at": "2026-06-01T13:00:00Z",
  "title_template": "球员 + 事件 + 结果",
  "clips": [
    {
      "id": "uuid-v4",
      "in": 252.0,
      "out": 262.0,
      "title": "防守没卡住人",
      "order": 0,
      "created_at": "2026-06-01T12:01:00Z"
    }
  ]
}
```

### 7.2 应用设置 `~/Library/Application Support/FootballClipper/settings.json`

```json
{
  "anthropic_api_key": "sk-ant-...",
  "title_template": "球员（宽宽/康康/浩浩/王悦恒）+ 动作 + 结果，25字内，失误直说",
  "default_player_names": ["宽宽", "康康", "浩浩", "王悦恒"],
  "ffmpeg_path": "bundled",
  "export_default_dir": "same_as_source"
}
```

---

## 八、自动保存规则

**触发点**（任何一项变化都触发）：
- I/O 拖拽结束
- 标题编辑失焦
- 片段删除
- 片段重排
- 新片段保存

**实现**：防抖 500ms 后写入 `.kkfb.json`

**反馈**：底部状态栏「已保存 14:32:05」实时更新

**容灾**：写入采用临时文件 + 原子 rename，避免崩溃导致 JSON 损坏

---

## 九、AI 文案整理 Prompt

```
你是青少年足球比赛视频片段的标题整理助手。
用户会用口语描述刚刚标记的片段，请整理成简洁标题。

规则：
- 优先包含：球员名（{player_names}）+ 动作 + 结果
- 控制在 25 字以内
- 不加引号、句号、表情
- 失误也直说，不美化
- 如果用户原话已经很规范，直接返回原话

用户原话：「{voice_text}」
返回：仅一个标题字符串，不加任何解释或前后缀。
```

**调用方式**：Claude API（`claude-haiku-4-5` 即可，速度快、便宜），用户填自己的 Anthropic API Key。

---

## 十、开发节奏（建议给 Claude Code 的迭代步骤）

| 阶段 | 内容 | 估时 | 验收 |
|---|---|---|---|
| 1 | 项目脚手架（electron-vite + React + TS + Tailwind + Zustand） | 0.5d | `npm run dev` 跑起来空白窗口 |
| 2 | 菜单打开视频 + HTML5 播放器 + 基础控制 | 0.5d | 能打开本地 mp4 并播放 |
| 3 | 时间线组件 + 游标 + 缩放 + 快捷键 | 1d | J/K/L、←→、Shift+←→、Space 都能用 |
| 4 | I/O 标记 + 浮窗显示 + Enter 保存 + 标题输入框 | 1d | 能标一个片段并出现在列表 |
| 5 | 片段列表（增删改 + 拖动排序 + 点击跳转） | 1d | 点击列表跳转到入点并选中 |
| 6 | I/O 拖拽调整 + 色条端点拖拽 + 自动保存 JSON | 1d | 拖完自动写盘 |
| 7 | ffmpeg sidecar 打包 + 批量导出弹窗 + 进度条 | 1.5d | 5 个片段一键导出 |
| 8 | Web Speech 语音输入 + Claude API 整理标题 | 1d | 按住 M 说话出现标题 |
| 9 | 设置页（API Key、标题模板）+ 状态栏 | 0.5d | Key 持久化 |
| 10 | 打磨 + 错误处理 + 打包 .dmg | 1d | 双击 dmg 安装即用 |

**合计 9 天纯开发时间**，断续来做约 2-3 周。

---

## 十一、给 Claude Code 的初始 Prompt

```
帮我开发一个 macOS 桌面应用 "FootballClipper"，给足球家长用的本地视频标记+剪辑工具。

技术栈：
- Electron 30+ + React 18 + TypeScript + Tailwind CSS + Zustand
- 使用 electron-vite 脚手架
- 视频用 HTML5 <video>
- 剪辑用 ffmpeg sidecar 二进制
- 项目文件用 JSON

详细规格见我提供的 FootballClipper-v1-规格.md 文档。

第一阶段任务：
1. 用 electron-vite 初始化项目
2. 集成 React + TypeScript + Tailwind + Zustand
3. 实现基础窗口和菜单（菜单包含 "打开视频" 项）
4. 主界面左右分栏：左侧上方是视频播放器，下方占位时间线区域；右侧是空白片段列表
5. 菜单 "打开视频" 能选本地 mp4 文件，加载到左侧播放器

完成后让我运行 `npm run dev` 看效果，然后告诉我下一步。
不要超前实现后续阶段的功能。
```

---

## 十二、风险与待定项

**已知风险**
- ffmpeg `-c copy` 只能在关键帧切，实际剪辑点可能与设置时间有 1-2 秒偏差 → 已确认对家长场景够用
- Web Speech API 需要联网（用 Google 服务），离线场景需后续替换为本地 Whisper
- 大视频（>2GB）的时间线缩略图生成性能 → v1 不做缩略图，只画色条

**v2 候选功能**（v1 完成后再评估）
- 球员标签和分类筛选
- 帧级精确剪辑选项（重编码模式）
- 缩略图条
- 多语言界面（先做中文）
- 一键发送到微信/AirDrop
- 球队空间（封闭式分享，不是公开社区）

---

**v1 完成的判定**：用 DJI_20260531162117_0023_D.MP4 这场比赛，10 分钟内标完 30 个片段并一键导出，全程键盘鼠标顺畅、无卡顿、无丢数据，AI 整理标题命中率 >70%。

到此为止，等你拍板就可以喂给 Claude Code 开工。
